---
machine:
  php:
    version: 5.5.21
dependencies:
  cache_directories:
    - ~/.composer/cache
test:
  override:
    - vendor/bin/phpunit --log-junit $CIRCLE_TEST_REPORTS/junit.xml
deployment:
  production:
    branch: master
    commands:
      - ssh farnswor@farnsworth.ch 'cd /home/farnswor/public_html/trnsprt.ch && git pull --rebase --stat && php -d allow_url_fopen=on -d disable_functions=exec composer.phar install --no-dev --no-interaction && chmod -R u+rwX,go+rX,go-w .'
